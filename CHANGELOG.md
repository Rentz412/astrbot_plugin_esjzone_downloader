# 更新日志 - Release

## v2.2.0 - 2026-06-08

### 安全

- 登录态存储改为仅加密保存 Cookie，不再持久化保存邮箱明文、密码明文或密码密文。
- Cookie 失效后不再使用本地保存的账号密码自动刷新，改为要求用户重新私聊登录。
- 启动和读取旧认证文件时会自动移除历史遗留的 `email_encrypted` / `password_encrypted` 字段。
- 认证调试页面保存默认关闭；认证调试文本和 JSON 现在需要同时开启 `debug.enabled` 与 `debug.save_auth_pages` 才会落盘。
- `.gitignore` 增加运行时隐私数据目录和认证密钥忽略规则，降低误提交风险。

### 文档

- README 补充登录态与隐私安全说明，明确账号密码仅用于本次登录请求、不再持久化保存。

## v2.1.0 - 2026-06-07

### 新增

- 新增 `/esj f` / `/esj favor` / `/esj favorite` 查看个人 ESJZone 收藏列表。
- 收藏列表支持多页抓取、用户级缓存、10 分钟被动刷新、1 分钟手动刷新冷却。
- 收藏列表优先使用合并转发发送，发送失败时自动降级为普通文本。
- 新增 `favorite` 配置段，可配置刷新间隔、手动刷新冷却、每页展示数量、最大抓取页数和 HTML 调试缓存。

### 修复

- 修复收藏列表合并转发被拆成多条消息、导致刷屏的问题。

### 文档

- README 增加收藏列表命令、配置项、缓存目录和隐私说明。

## v2.0.0 - 2026-06-02

### 新增

- 新增基于 AstrBot Plugin Page 的 Dashboard 可视化管理界面。
- Dashboard 支持查看本地书库统计、本地小说卡片、书籍详情、封面、导出文件和占用空间。
- Dashboard 支持搜索本地书库，并提供清理调试文件、清除导出文件、删除书籍数据等管理操作。

### 文档

- README 增加 Dashboard 功能、使用方式、数据来源和安全注意事项说明。

## v1.3.0 - 2026-06-01

### 调整

- 移除旧的独立 WebUI / Dashboard 配置、启动关闭命令和前端页面遗留物。
- 删除 `/esj db on|off|status` 命令及相关 host、port、token 配置。
- 为后续基于 AstrBot Plugin Pages 重新开发 Dashboard 做准备。

## v1.2.0 - 2026-06-01

### 修复

- 修复 `/esj d <编号> <起始章节> [结束章节]` 省略格式参数时会被误判为导出格式的问题。

### 调整

- 自定义章节下载现在可以省略格式参数，并自动使用配置中的默认导出格式。

## v1.1.0 - 2026-06-01

### 修复

- 修复下载命令在本地已存在导出文件时，可能发送错误章节范围文件的问题。
- 修复部分章节导出时可能混入其他已缓存章节的问题。
- 修复不同章节范围的 ZIP 压缩包可能互相覆盖或混淆的问题。

### 调整

- 导出文件现在按格式和章节范围区分，例如全本与 `0002-0015` 会生成不同文件。
- 每个导出文件会保存对应的 manifest，用于判断本次请求是否可以安全复用本地导出缓存。
- 下载时会优先复用已缓存章节，只补下载缺失章节。
- ZIP 作为临时发送产物，发送尝试结束后会自动清理；章节缓存、导出文件和元数据会继续保留。
- 下载命令仍会先获取网站最新章节目录；如果网站章节目录发生变化，会清理旧运行时缓存并重新生成文件。